schema_version: 1

context:
  name: typescript-eslint-eslint-plugin
  version: "8.45.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/typescript-eslint/typescript-eslint/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 9a0ccfb7d7d20376443123ae876b33a7549231d4178dffc625a9396fac2cc1ac

build:
  number: 0
  skip: win

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - make
    - nodejs
  host:
    - nodejs
    - pnpm
    - pnpm-licenses
    - yarn
  run:
    - nodejs

tests:
  - script:
      - if: unix
        then: test -d $PREFIX/lib/node_modules/@typescript-eslint/eslint-plugin
      - if: win
        then: if not exist %LIBRARY_INC%\\lib\\node_modules\\@typescript-eslint\\eslint-plugin 1

about:
  license: MIT
  license_file:
    - LICENSE
    - packages/eslint-plugin/ThirdPartyLicenses.txt
  summary: Find and fix problems in your JavaScript code.
  homepage: https://github.com/typescript-eslint/typescript-eslint

extra:
  recipe-maintainers:
    - xhochy
